# 検証結果

## 自動確認

- TypeScript型チェックとVite本番ビルド。
- モデルテスト8件：バックアップ往復、形式・参照関係・バージョン検証、グループ削除時の移動、項目削除時の割り当て解除、日本語検索、並び替え、構文エラー、同期／非同期例外。
- UI操作：項目追加、再読み込み後の保存、検索、グループ移動、カード／リスト・幅変更、テーマ適用、狭い画面、検索ポップアップ。
- 追加UI操作：カスタム配色保存、ページタイトル・列数、直接実行30枠目、JSON書き出し、不正インポートで既存データを保持、確認後の復元、ドラッグハンドルによる別グループへの移動、不正URL拒否。
- 検証用Chromiumへの拡張読み込み：Service Worker起動、31コマンドの登録、Chromeストレージ永続化、同時保存競合、実行禁止ページ、権限不足。
- 実行テスト：ユーザースクリプト許可トグル、MAIN worldでのDOM変更、同期／非同期例外・構文エラー・対象タブ消失の表示。テスト用コピーだけにホスト権限を事前付与し、配布版の任意権限設計は変更していません。
- ダッシュボード、テーマ設定、狭い画面、ポップアップのスクリーンショットを目視確認。

## ユーザー環境での初回確認

- Chromeのネイティブ権限ダイアログでの承認は、ヘッドレス自動テストでは行っていません。READMEの手順で許可してください。
- キーの物理入力による直接実行は、OS・Chrome・他の拡張機能との競合があるため、割り当て後に使用環境で確認してください。コマンドの登録と各枠の保存は検証済みです。
- あらゆる第三者サイト／既存ブックマークレットとの互換性を保証するものではありません。ページ側CSP、外部ライブラリ、遅延タイマー等の制限はREADMEに記載しています。

## 再実行

`npm run dev -- --port 4173` を起動し、別ターミナルで次を実行します。

```sh
npm run build
npm test
node tests/ui.mjs
node tests/features.mjs
node tests/extension.mjs
node tests/execution.mjs
```

`CHROME_PATH` で拡張機能を読み込めるChromiumの実行ファイルを指定できます。テスト用プロファイルはプロジェクト外の相対パス `../../work/` 以下に作成します。ユーザーの通常のChromeプロファイルは変更しません。
